#!/bin/sh

branch_name="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch_name" = "main" ] || [ "$branch_name" = "master" ]; then
  echo "[STOP] Commit bloccato su $branch_name."
  echo "Crea prima un branch dedicato: npm run start-branch -- fix/nome-branch"
  exit 1
fi

exit 0
